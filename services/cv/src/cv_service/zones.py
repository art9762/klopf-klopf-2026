"""Zone geometry: entry/exit lines for sides A and B + midzone polygon.

TODO: load JSON config, expose helpers `crossed_entry(side, prev, cur)`,
`crossed_exit(side, prev, cur)`, `inside_midzone(point)`.
"""

from __future__ import annotations
