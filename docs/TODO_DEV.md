# Задачи: Разработка (AI/Backend/Frontend)

## Приоритет 1 — CV сервис (критический путь)

### 1. Базовый CV pipeline
- [ ] Настроить YOLOv8n + ByteTrack (ultralytics + supervision)
- [ ] Найти/записать 3 видео: baseline (спокойный поток), rush_hour (асимметрия), stuck_truck (фура)
- [ ] Реализовать zone_picker.py — утилита для разметки линий въезда/выезда мышью
- [ ] Реализовать подсчёт пересечений виртуальных линий → публикация entry/exit в MQTT
- [ ] Реализовать midzone: список ТС внутри полигона зоны, stuck_ids (скорость bbox < порог >30с)

### 2. Интеграция CV с Control
- [ ] Подписка CV на `traffic/cmd/scenario` — запуск нужного видео
- [ ] Флаг `--speed` для ускорения/замедления воспроизведения
- [ ] Проверить end-to-end: видео → CV → MQTT → Control → WebSocket → Dashboard

## Приоритет 2 — Backend доработки

### 3. Replay script улучшения
- [ ] Добавить реалистичные паттерны в replay.py (волны трафика, не равномерный поток)
- [ ] Поддержать все 5 сценариев (baseline, rush_hour, stuck_truck, emergency, comm_loss)
- [ ] Автозапуск replay при старте если CV не подключён

### 4. Метрики throughput
- [ ] Считать реальный throughput_per_hour (сейчас всегда 0 без CV)
- [ ] Записывать метрики в InfluxDB для исторического сравнения
- [ ] GET /metrics/compare — отдавать реальные данные из InfluxDB

### 5. Сессии сценариев
- [ ] При старте сценария сбрасывать метрики
- [ ] При завершении — сохранять итоговые метрики в SQLite
- [ ] Поддержать последовательный запуск fixed → adaptive для одного видео

## Приоритет 3 — Frontend доработки

### 6. UI polish
- [ ] Добавить анимацию машин проезжающих через зону (сейчас статичные)
- [ ] Показывать countdown до смены фазы (phase_will_end_at)
- [ ] Добавить звуковой сигнал при смене фазы (опционально)
- [ ] Split-view: fixed и adaptive рядом в реальном времени

### 7. Презентация
- [ ] Кнопка "О проекте" — краткое описание для жюри прямо в дашборде
- [ ] Автоматический запуск демо-сценария при первом открытии
- [ ] Скриншоты/GIF для README

### 8. Тесты
- [ ] E2E тест: replay.py → control → проверить что WS-клиент получает данные
- [ ] Тест на safety: 1000 итераций replay без unsafe switches
- [ ] Frontend: тест на PhaseTimeline и MetricsPanel с моковыми данными

---

## Порядок работы

```
CV pipeline (1-2) → Интеграция (2) → Replay улучшения (3) → Метрики (4) → UI polish (6) → Презентация (7)
```

**Дедлайн feature freeze:** за 8 часов до финала. После — только баги и полировка.
